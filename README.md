# Python Examples

## Get Started (after setup)
```
source venv/bin/activate
make main
```

## Setup (do once)
```
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```# py_templates
